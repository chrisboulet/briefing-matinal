# xAI Grok API key — injectée par hermes-agent en production
XAI_API_KEY=

# production | staging | dry-run
BRIEFING_ENV=dry-run

# Hostname Tailscale du redirector (HTTPS via cert tailscale)
TAILNET_HOSTNAME=hermes.example.ts.net

# Chemin NAS pour fallback archivage
NAS_PATH=/mnt/nas/commun/briefing-matinal

# Path vers la DB SQLite du redirector
TRACKER_DB_PATH=./briefing_tracker.db
